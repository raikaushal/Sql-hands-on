-- DDL data defination language
-- statements which defines schemas or data is called data defination language
-- e.g.
CREATE TABLE tasks(task_name VARCHAR(255), complete CHAR(1));
-- DML data manipulation language
-- statements which manipualtes data add update or remove
-- e.g.
SELECT
  *
FROM
  tasks